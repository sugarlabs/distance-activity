ss = 'test from server'
sc = 'test from client'

def measure_dt_seq(soc, is_server):
    if is_server:
        return self.server(soc)
    else:
        return self.client(soc)

def server(soc):
    soc.sendall(ss)
    x = soc.recv(len(sc))
    return float(hash(x))

def client(soc):
    x = soc.recv(len(ss))
    soc.sendall(sc)
    return float(hash(x))

def speed_of_sound():
    return 345.67
